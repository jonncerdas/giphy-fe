import reducer, { handleFavorites } from '../../../features/giphy/giphySlice'
import { ResponseObject } from "../../../common/types";

// test('should return the initial state', () => {
//   expect(reducer(undefined, {data, pagination, meta}: ResponseObject)).toEqual([
//     {
//       text: 'Use Redux',
//       completed: false,
//       id: 0
//     }
//   ])
// })